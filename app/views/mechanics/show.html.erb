<h1><%= @mechanic.name %> Page</h1>
<p>Years of Experience: <%= @mechanic.years_experience %></p>
<h2>Rides Working On:</h2>
<ul>
  <% @mechanic.open_and_thrill_order.each do |r| %>
    <li><%= r.name %></li>
  <% end %>
</ul>
<h2>Add a Ride to their Workload:</h2>
<%= form_with model: @mechanic, local: true do |f| %>
  <%= f.label :ride_id, 'Ride ID:' %>
  <%= f.number_field :ride_id %>
  <%= f.submit 'Submit' %>
<% end %>